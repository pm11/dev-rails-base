db:
  image: mysql:5.6
  ports:
    - 3306
  environment:
    MYSQL_ROOT_PASSWORD: password
web:
  build: .
  command: bundle exec rails server -p 3000 -b '0.0.0.0'
  volumes:
    - .:/var/myapp
  ports:
    - 3000:3000
  links:
    - db

